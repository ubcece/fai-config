#! /usr/sbin/cfagent -f

control:
   any::
   actionsequence = ( editfiles )
   EditFileSize = ( 10000 )

editfiles:
   any::
        { ${target}/etc/inittab
          CommentLinesMatching ".*getty .* tty[2-9]$"
        }

#! /bin/bash
# 2009-04-27 Chris Dumont. install grub early so no errors during instsoft

ifclass SQUEEZE && exit 0

trap "echo this was expected." ERR

fcopy -Uv boot/grub/menu.lst
# We expect this to fail since postinst attempts to run
# $target/usr/sbin/update-grub which is not installed yet.
exit 0

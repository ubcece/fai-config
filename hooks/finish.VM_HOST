#!/bin/sh
# 2009-10-26 Derek Poon <derekp@ece.ubc.ca>

# pve-kernel-2.6.24-8-pve postinst script calls "update-grub -y" unless
# a /proxmod_install_mode file is present.  The -y option is deprecated
# in GRUB and is no longer tolerated in GRUB 2.
#
# The prepareapt hook created this file; let's clean it up now.
rm -f $target/proxmox_install_mode

# Run the update-grub that was deferred
( ifclass GRUB || ifclass GRUB2 ) && $ROOTCMD /usr/sbin/update-grub

#!/bin/sh

# Remove LVM logical volumes named FAI_DELETEME*.  The disk configuration may
# include such logical volumes to reserve space in the volume group for making
# snapshots.
for lv in /dev/mapper/*-FAI_DELETEME* ; do
    if [ -b "$lv" ]; then
        lvremove -f "$lv"
    fi
done

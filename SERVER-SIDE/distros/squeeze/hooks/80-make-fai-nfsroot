# make-fai-nfsroot, in copy_fai_files(), removes /etc/fai/make-fai-nfsroot.conf
# from the NFS root.  Restore it so that dirinstall can run from the NFS root.
cp "$cfdir"/make-fai-nfsroot.conf "$NFSROOT"/etc/fai/

#! /bin/bash

echo FAIBASE
echo DHCPC

case $HOSTNAME in
    svmailp[1-5])
	echo "ECE SLOW UBUNTU UBUNTU_MAIL_IMAP" ;;
    smtp?|svsmtpp?)
        echo "ECE MAIL" ;;
    svwebmailp[1-2])
	echo "ECE SLOW UBUNTU STANDARD" ;;
    blanc|puncak|vinson|xena9)
	echo "ECE SLOW UBUNTU STANDARD" ;;
    [kmxv]sql?)
        echo "ECE WEB DATABASE RT RAID" ;;
    svsql[dp]*)
        echo "ECE UBUNTU STANDARD WEB DATABASE RT" ;;
    lersse1)
        # lersse1 is integrated with ECE but is limited to mounting netapp4:/vol/ECE_vol3/kb/data/backup.
        echo "ECE GNOME KDE OMNI SLOW " ;;
    modigliani)
        # modigliani is not integrated with ECE.
        echo "GNOME KDE OMNI" ;;
    [kmx]host*)
        echo "ECE VM_HOST" ;;
    [kmxv]ldap?|master)
        echo "ECE LOOKUPSERVER" ;;
    [kmxv]lookup?)
        echo "ECE LOOKUPSERVER VERTXSERVER" ;;
    svlookupp?)
        echo "ECE LOOKUPSERVER" ;;
    msmb1)
        echo "ECE SMBSERVER RAID " ;;
    [kmxv]smb?|svsmbp1)
        echo "ECE SMBSERVER" ;;
    [kmxv]web?|[kmx]vweb?|svweb*)
        echo "ECE WEB RT" ;;
    [kmxv]mx?|[kmx]mail?|*smtp*)
        echo "ECE MAIL WEB" ;;
    [kmxv]flex?|vintr1|yoink)
        echo "ECE " ;;
    psserv|ssh-linux*|vb|vklinux[345])
        echo "ECE GNOME KDE OMNI" ;;
    node*) # to be clear: these are nodes on Rafeef/morty.
        echo "SLOW ECE GRID GNOME KDE OMNI" ;;
    morty)
        echo "SLOW RAID ECE GRID GNOME KDE OMNI" ;;
    *webgoat*) 
        echo "ECE UBUNTU WEBGOAT" ;;
    purang1[345])
        echo "ECE VM_HOST GNOME KDE OMNI" ;;
    frink)
        echo "ECE GNOME KDE OMNI " ;;
    *)
        echo "SLOW ECE GNOME KDE OMNI" ;;
esac

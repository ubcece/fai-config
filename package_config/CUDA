# Workstation for CUDA (GPGPU) development, with prerequisites for Caffe Deep
# Learning <http://caffe.berkeleyvision.org/install_apt.html>

PACKAGES install UBUNTU_14_04
cuda-repo-ubuntu1404

PACKAGES install
cuda

# Caffe itself is not available as a DPKG, and therefore we expect users
# to build it themselves in /data.  However, we can install these dependencies.

# General dependencies for Caffe
PACKAGES install UBUNTU
libprotobuf-dev
libleveldb-dev
libsnappy-dev
libopencv-dev
libboost-all-dev
libhdf5-serial-dev
protobuf-compiler

# ... that require Ubuntu >= 14.04
PACKAGES install UBUNTU
libgflags-dev
libgoogle-glog-dev
liblmdb-dev

# ATLAS / BLAS.  BLAS is reputedely faster than ATLAS, but we'll offer both
# and let the user decide
PACKAGES install UBUNTU
libatlas-base-dev
libopenblas-dev

# Related packages for convenience
PACKAGES install UBUNTU
cython
cython3
h5utils
hdf5-tools
libleveldb-dbg
libleveldb-dev
lmdb-utils
protobuf-c-compiler
python3-leveldb
python-leveldb
python-protobuf
python-snappy
python-skimage
python3-skimage
python-h5py
python3-h5py
python-networkx
python3-networkx
python-nose
python3-nose
python-nose2
python3-nose2
python-pil
python3-pil
